<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- 상단 섹션 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <!-- 이미지 -->
            <ImageView
                android:id="@+id/ivPatentImage"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/ic_placeholder_image"
                android:scaleType="centerCrop"
                android:layout_marginEnd="16dp" />

            <!-- 제목 -->
            <TextView
                android:id="@+id/tvPatentTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="20sp"
                android:textStyle="bold"
                android:text="Title Placeholder"
                android:layout_marginEnd="8dp" />
        </LinearLayout>

        <!-- 상세 설명 -->
        <TextView
            android:id="@+id/tvPatentSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:layout_marginBottom="16dp"
            android:text="Summary Placeholder" />

        <!-- 기타 정보 -->
        <TextView
            android:id="@+id/tvApplicationDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:text="Application Date: Placeholder"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/tvInventor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:text="Inventor: Placeholder"
            android:layout_marginBottom="8dp" />

    </LinearLayout>
</ScrollView>
